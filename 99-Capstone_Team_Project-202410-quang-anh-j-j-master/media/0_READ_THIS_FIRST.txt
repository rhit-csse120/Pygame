Put your project's MEDIA (image, sound, ...) files in this folder.
Reference them from your  src  folder per this example
of code from a file in the  src  folder:

   self.image = pygame.image.load("../media/fighter.png")
